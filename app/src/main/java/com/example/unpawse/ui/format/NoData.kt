package com.example.unpawse.ui.format

/**
 * Shown where a metric has nothing behind it yet. A blank slot is not the same as a zero, and one
 * numeric slot can't say both — "0m Remaining" on a fresh install read identically to a budget the
 * user had just burned through.
 */
const val NO_DATA = "—"
